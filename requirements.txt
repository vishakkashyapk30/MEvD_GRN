# ── Core scientific stack ─────────────────────────────────────────────
numpy>=1.24,<2.0
scipy>=1.11
pandas>=2.0
pyyaml>=6.0
tqdm>=4.65

# ── Single-cell ───────────────────────────────────────────────────────
scanpy>=1.9.6
anndata>=0.10

# ── Deep learning ─────────────────────────────────────────────────────
# Install torch FIRST, matched to the cluster CUDA, e.g.:
#   pip install torch==2.1.0 --index-url https://download.pytorch.org/whl/cu118
# Then PyG + its sparse ops from the matching wheel index:
#   pip install torch_geometric==2.4.0 \
#       torch_scatter torch_sparse \
#       -f https://data.pyg.org/whl/torch-2.1.0+cu118.html
torch>=2.1
torch-geometric>=2.4

# ── Genomic interval ops (peak -> gene mapping) ───────────────────────
pyranges>=0.0.129
pybiomart>=0.2.0          # optional online TSS source; offline GTF preferred on Ada

# ── Evaluation / plotting ─────────────────────────────────────────────
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.12

# ── Baselines (optional; install on demand) ───────────────────────────
# arboreto>=0.1.6         # GRNBoost2 / GENIE3
# regdiffusion            # fastest DL baseline
